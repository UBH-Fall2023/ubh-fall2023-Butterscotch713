
grid=document.getElementById("grid").getContext("2d"); //get the grid element from HTML file

addEventListener("click", (event) => {
    //moving script here
    contextBridge.translate(x, y)
    

});
